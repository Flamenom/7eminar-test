import 'bootstrap/dist/js/bootstrap.bundle.min.js'

export default defineNuxtPlugin(() => {
  // Плагин инициализируется только на клиентской стороне
  // Здесь можно добавить дополнительную конфигурацию Bootstrap
}) 